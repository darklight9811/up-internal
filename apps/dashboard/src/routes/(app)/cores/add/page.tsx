import { metadata } from "@repo/domains/app";

export const meta = metadata({ title: "Adicionar Núcleo" });

export default function CoreAddPage() {
	return (
		<main className="p-4 pt-0">
			<h1 className="text-3xl font-bold">Adicionar núcleo</h1>
		</main>
	);
}
